name: CI

on:
   workflow_dispatch:

jobs:
   build:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@v3
       - name: Run Stuff And Build
         run: |
          sudo apt-get update
          sudo apt-get upgrade
          sudo apt install curl awk grep openjdk-17-jdk openjdk-17-jre
